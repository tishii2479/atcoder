#include <algorithm>
#include <cmath>
#include <iostream>
#include <map>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <vector>
using namespace std;
typedef long long ll;
#define rep(i, n) for (int i = 0; i < n; i++)
#define repd(i, n) for (int i = n - 1; i >= 0; i--)
#define MOD 1000000007
// #define MOD 998244353
const ll INF = 1ll << 60;
const int inf = 1 << 29;
int main() {
    double sx, sy, gx, gy;
    cin >> sx >> sy >> gx >> gy;
    double ans = (gx - sx) * (abs(sy) / (abs(sy) + abs(gy))) + sx;
    printf("%.10lf\n", ans);
}